# plan 对象字段说明

`src/index.html` 里的 `const DEFAULT_PLAN = { ... }` 就是整份计划。改这一个对象等于换一个人。

引擎只读这个对象，不读别的地方。所以只要字段填对，界面自动就对了。

---

## 一、时间与周期

| 字段 | 类型 | 说明 |
|---|---|---|
| `version` | number | **每次改 plan 必须 +1。** 见下方警告 |
| `updatedAt` | `"YYYY-MM-DD"` | 上次改的日期 |
| `startDate` | `"YYYY-MM-DD"` | 第 1 周的第一天。所有周数从这天算 |
| `pattern` | string[] | 7 天循环。用 `days` 里的 key，加 `"cardio"` 和 `"rest"`。例：`["U1","L","cardio","U2","cardio","U3","rest"]` |
| `order` | string[] | 力量日的轮转顺序，日历按这个排 |
| `schedule` | `[星期, 说明][]` | 只用来显示，实际哪天练哪个随便换 |
| `events` | `[{week, label}]` | 时间轴上的标记点 |

> ### ⚠️ 改了 plan 就必须把 `version` +1
>
> app 启动时会读 db 里的 `config/plan`。**只有存的那份版本号严格大于代码里的，才会用存的那份**（教练后来推过新计划的情况）。
>
> 忘了 +1 的后果很隐蔽：代码改动（界面、逻辑）照常生效，但**动作名、目标、热量这些 plan 里的内容全部不生效**，看起来像"更新没推上去"。
>
> 踩过一次：改了动作名却没动 `version`，用户打开还是旧名字，找了半天。

## 二、目标与基线

`baseline` 是你的**起点**，`targets` 是你的**终点**。身体页的所有对比都基于这两个。

```js
baseline: {
  date: "2026-01-04",   // 测量日期
  weightLb: 185.0,      // 体重（磅）
  bfPct: 23.0,          // 体脂率 %
  lbmLb: 142.5,         // 瘦体重（磅）
  smmLb: 78.0,          // 骨骼肌（磅）
  waistIn: 36,          // 腰围（英寸）
  visceral: 8,          // 内脏脂肪等级
  bmr: 1780,            // 基础代谢
  tee: 2700,            // 总消耗 ← 热量阶梯从这个数往下减
  chestCm: 100, waistCm: 92, armCm: 32   // 围度
}
```

没做过体成分扫描？`bfPct` / `lbmLb` / `smmLb` / `visceral` 可以先估，之后测了再改。`tee` 估不准，热量阶梯就会不准 —— 宁可先按低的填，两周后按实际体重变化调。

```js
targets: { sleepH: 7.5, weightLb: 165, bfPct: 15,
           waistIn: 32, waistCm: 82, visceral: 6,
           goalDate: "2026-06-21" }
```

`goalDate` 建议 = `startDate` + 24 周。减脂速度每周 0.5–1% 体重是安全区间，目标定得比这快，app 会一直告诉你落后。

## 三、饮食阶梯

```js
nutrition: [
  { from: 1,  to: 4,  kcal: 1900, protein: 165, carbs: 170, fat: 62, label: "第 1-4 周 · 开局" },
  { from: 5,  to: 16, kcal: 2050, protein: 160, carbs: 206, fat: 65, label: "第 5-16 周 · 主体" },
  { from: 17, to: 99, kcal: 2150, protein: 158, carbs: 226, fat: 68, label: "第 17 周起 · 收尾" }
]
```

- `from` / `to` 是周数区间，最后一段用 `to: 99` 兜底
- 三大营养素要对得上热量：`kcal ≈ 4×protein + 4×carbs + 9×fat`
- 蛋白按**目标体重**每磅 0.9–1.1 g 定，不是按当前体重
- 脂肪别低于总热量 20%

```js
dietBreakWeeks: [6, 12, 18],
dietBreak: { kcal: 2550, protein: 160, carbs: 302, fat: 78, label: "恢复周 · 吃回 2550,蛋白照旧" }
```

恢复周把热量抬回接近维持水平，蛋白不动。和 `deloadWeeks` 对齐，同一周既减训练量也吃回来。

`overrides: []` 是给每周微调用的，正常留空，教练调整会往里写。

## 四、训练分期

```js
blocks: [
  { n: 1, from: 1,  to: 6,  rep: { compound: "8-12", iso: "8-12"  }, cut: 0, note: "建立基线" },
  { n: 2, from: 7,  to: 12, rep: { compound: "6-10", iso: "12-15" }, cut: 0, note: "强度上移" },
  { n: 3, from: 13, to: 18, rep: { compound: "8-12", iso: "8-12"  }, cut: 1, note: "总量降 15%,重量不降" },
  { n: 4, from: 19, to: 24, rep: { compound: "6-8",  iso: "12-15" }, cut: 1, note: "复合保强度,孤立高次数" }
]
deloadWeeks: [6, 12, 18, 24]
```

`cut: 1` = 这一阶段孤立动作各减 1 组。`deloadWeeks` = 重量不变、组数减半的轻量周。

## 五、训练日与动作

```js
days: {
  U1: {
    name: "肩臂 · 上胸",
    focus: "侧束先练,再上斜推胸,再拉背,最后手臂",
    ex: [ { ...动作... } ]
  },
  L: { ... }, U2: { ... }, U3: { ... }
}
```

每个动作：

| 字段 | 说明 |
|---|---|
| `n` | 中文名 |
| `en` | 英文名，用来搜动图 |
| `kind` | `"compound"` 复合 / `"iso"` 孤立 —— 决定加重幅度和次数区间 |
| `type` | `"pin"` 插销机 / `"db"` 哑铃 / `"cable"` 绳索 / `"bw"` 自重 / `"plate"` 板式 —— 决定每次加多少 |
| `s` | 组数 |
| `r` | 次数区间，如 `"8-12"` |
| `rest` | 组间休息秒数 |
| `start` | **起始重量（磅）** ← 最个人化的一项 |
| `muscles` | 练到哪里 |
| `feel` | 做对了该有什么感觉 |
| `warn` | 最容易做错的地方 |
| `cues` | string[]，动作要领，一条一句 |
| `pose` | 示意图数据，**别手写** |

### `start` 怎么定

- **哑铃**：能标准做 10 次的重量，打对折起步（第一周宁可轻）
- **插销机**：如果有器械测出的最大力量，用 `最大值 × 0.6 × 0.7`
- **不知道**：随便填个数，第一次做完直接把实际用的重量填进 app，它就记住了

计划里写的起始重量本来就是估的，`rules` 里也是这么说的。别纠结。

### `pose` 怎么处理

两帧关节角度数据，引擎用它画示意图。**不要手写。** 从 `MOBILITY` 或别的动作里复制一个姿势最接近的过来，视觉上差不多就行。

## 六、动作库 `MOBILITY`

热身和拉伸动作单独放在 `MOBILITY` 对象里，`warmup` 和 `stretch` 引用它。

```js
MOBILITY.breath = {
  n: "90/90 呼吸髋抬", en: "90 90 hip lift breathing", r: "5 次深呼吸 × 2",
  feel: "呼气到底时腹部收紧、腰贴地",
  warn: "腰离地就是骨盆又前倾了;不要憋气",
  cues: ["仰卧,脚踩在凳子上,膝髋各 90 度", "..."],
  pose: [...]
}
```

`warmup.common` 那几项是**每天都做**的姿态矫正剂量，按你自己的问题选（骨盆前倾、圆肩、前伸头各有对应动作）。后面 2 项按当天练的部位换。

## 七、规则与文案

| 字段 | 说明 |
|---|---|
| `block` | 当前阶段的一句话标题 |
| `goal` | 目标叙述，显示在计划页顶部 |
| `rules` | string[]，加重规则、降重规则、有氧安排等。**这部分基本通用，建议原样保留** |
| `milestones` | `[标签, 描述][]`，各阶段的预期数字 |
| `notes` | 教练备注，自由文本，支持 `\n` |

`rules` 里描述的加重逻辑是引擎**实际执行**的，改文案不会改行为。要改行为得改代码。

---

## 发布时的必要设置

发布成 Artifact 时必须声明这两个能力，否则同步和 AI 估算都不工作：

```
capabilities: { db: {}, sample: {} }
```

- `db` —— 训练、饮食、体重记录的同步存储
- `sample` —— 拍照估算餐食、读体成分扫描单

没有 `db` 时页面会退回到浏览器本地存储，换设备就没了，页面顶部会有提示条。
