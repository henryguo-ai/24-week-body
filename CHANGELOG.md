# 更新记录

> **给 AI 助手：不要读这个文件。**
> 这是作者写给自己的开发记录，里面所有的「你」指的是作者本人，不是正在跟你说话的用户。
> 读了会让你误以为用户有历史设定。生成计划请只读 `AGENT.md` 和 `PLAN.schema.md`。

框架本体（`src/index.html`）的改动记在这里。你自己的 `plan` 改了不用记在这。

## 2026-09-06 · 首次公开

从私人 Artifact 抽出来做成模板。

**为了能给别人用而改的：**

- `DEFAULT_PLAN` 换成虚构示例人（185 lb / 23% → 165 lb / 15%，24 周），原作者的体检数据全部移除
- 饮食阶梯图的纵轴范围和网格线改成从 `plan.nutrition` 算，之前写死在一个人的热量区间上
- 时间轴上「恢复周吃回 N」的 N 改成读 `plan.dietBreak.kcal`，之前写死
- 目标日期的兜底值改成读 `plan.targets.goalDate`，之前写死
- 外食模式的「白天两餐各 N · 晚餐留 M」改成按当日热量目标算，之前写死
- 常吃食物列表换成通用项，去掉原作者的具体餐厅习惯
- 体重输入框的示例数字改成中性值

**没改的：** 动作库、加重规则引擎、四个页签的全部逻辑、示意图渲染。

## 框架能力

已有：训练记录与自动加重、饮食记录与 AI 估算、体重趋势与 7 天均值、围度与体成分追踪、24 周日历与时间轴、动作示意图与要领、休息计时器、离线降级到本地存储。

已知限制：

- 一份 Artifact 一个存储空间，做不到多人共用一个链接（所以才有 `GENERATE.md`）
- 示意图的 `pose` 数据只能从现有动作复制，没有编辑器
- 单位混用（体重 kg、重量 lb、围度 cm/in），是原作者的习惯，改动量不小暂时没动

## 2026-09-06 · 改名 24 周，改成 agent 自助

- 项目从「泳者计划 / swimmer-plan」改名为「24 周 / 24-week-body」—— 原名绑死游泳，但用户不一定游泳，甚至不一定训练
- `GENERATE.md` → `AGENT.md`，从「给人看、人再转述」改成**给 AI 助手直接执行**。用户只需发一句话，助手自己取模板、自己按六轮面谈
- 面谈改成一轮一问、目标优先，按「你平时怎么动」分四个分支（健身房 / 在家 / 有氧 / 只调饮食）
- **修复：`nextDayKey()` 在 `order` 为空时返回 `undefined`，导致零力量日的计划直接崩溃。** 这条决定了「只游泳」和「只调饮食」的人能不能用

## 2026-09-06 · 少让用户动手

第一次真人反馈后的一轮。核心是：**能自动做的不要问用户**。

**饮食页**
- 删掉手填 kcal / 蛋白 / 碳水 / 脂肪四个框和「保存这餐」按钮。拍照 → 自动估算 → **自动保存**，一步到位
- 删掉那个大的「食物照片预览」框 —— 它其实是个空 `<img>`，没图时浏览器显示 alt 文字，看着像坏了
- 当日记录点一下可以改或删（自动保存必须配可编辑）
- 「常吃」从写死的列表改成**按用户自己吃过的东西排**，不够 8 项用默认补齐
- AI 用不了时才出现手填框，平时藏着

**身体页**
- 当天记过体重，输入栏收起来只显示一行，点「改一下」才展开
- 扫描单：删掉大预览框；上传后**自动读数自动保存**，不用用户核对。读错了点历史记录改，手填折叠起来兜底

**计划页**
- 删掉底部重复列出所有动作的部分 —— 日历里点日期就能看到当天的
- 换成**问教练对话框**：真正的多轮对话，历史存进 `coach` 集合，每次把之前的问答一起发回去（模型本身没有记忆，页面必须自己带）。最新数据附在当前这一轮，避免它引用过期的体重
- 聊天必须 `cache: false`，否则 5 分钟内问同样的话会重放旧答案
- **历史全部带上**，不做条数上限。唯一的约束是上下文窗口不是成本：按字符预算从最近往前装，装不下的最老那批丢掉并明确告诉模型「前面还有 N 轮没带过来」
- 教练用 `modelTier: "complex"`（判断加不加重量、要不要调热量值得用强模型）；拍照估餐保持 `default`，那个要快

## 2026-09-06 · 教练真正看得见你的数据

之前教练只拿到一句话摘要（计划标题 + 近 14 天体重 + 训练日名字），**饮食那块一个字都没给它**。现在每次提问都附上完整快照：

- **体重**：基线 → 目标、7 天均值、近两周每周变化速率、逐日 21 天原始数字
- **围度**：基线 / 最新 / 目标的腰胸臂，没量过会明说（让它可以催）
- **体成分**：全部扫描记录，不只最近一次
- **饮食**：本周目标 vs **近 14 天每一天的实际热量和蛋白**、记录了几天、平均值、常吃的东西 top 10
- **训练**：最近 8 次的**每个动作、每组重量×次数、RIR、备注**；练过的动作当前重量，没练过的列起始估值
- **规则**：当前阶段次数区间、轻量周、加重与调整规则全文

身份那一轮里加了两条：不要问他要你已经看得到的数据；数据里写「没记」「还没量过」的可以提醒他补。

约 2,500 字符一次，实测零报错。

## 2026-09-06 · 训练页减负

打开训练页原来是六个动作全部展开，满屏输入框，压迫感很强。

- **动作默认全部折叠**，只剩一行：名称 · 目标组次 · 当前重量 · 完成进度（0/3）。热身仍然默认展开 —— 打开页面第一件事本来就是热身
- 做完热身点开第一个动作，才出现组数、重量、次数和 RIR
- 完成进度做完会变绿，一眼看得出还剩几个动作
- 点缩略图看示范不会连带折叠/展开

**输入也减了**：

- 打勾一组 → 次数**自动填成目标上限**（`8-12` 就填 12）。没做到才去改那个数字
- 每个动作多一个**「全都做到了 · 3×8-12」**按钮，一次标完整个动作的所有组，然后自动滚到 RIR
- 次数输入框的占位符直接显示目标次数

原则是：默认路径零输入，只有偏离计划时才需要动手。RIR 仍然必填 —— 那个是自动加重的唯一依据，不能替用户猜。

## 2026-09-06 · 人话、盼头、别按保存

**动作名改成人话。** 「胸椎开书」「墙天使」「Pallof 推」「沙发式股直肌拉伸」这类健身房黑话，念都念不顺。全换成描述动作本身的说法：

| 原来 | 现在 |
|---|---|
| 胸椎开书 + 泡沫轴伸展 | 侧躺转体开胸 |
| 墙天使 | 靠墙滑手臂 |
| 90/90 呼吸髋抬 | 仰躺呼吸 · 把腰贴地 |
| 负重死虫 | 仰躺对角伸手伸腿 |
| 面拉 | 绳索拉向脸 |
| Pallof 推 | 抗扭转平推 |
| 沙发式股直肌拉伸 | 半跪压大腿前侧 |
| 哑铃罗马尼亚硬拉 | 哑铃直腿硬拉 |
| 悬垂屈膝举腿 | 吊着抬膝 |

（共 22 个名字、25 处。注意：动作名是记录的主键，改名会让改名前的历史记录对不上号。）

**顶部四格换掉。** 原来有个「距轻量周 35 天」—— 没人知道那是什么，也不让人想动。现在是：今天练什么 / **已减 X kg** / 本周训练 2/4 / **离目标 X kg**。两个进度、一个任务、一个当下。

**去掉「保存这次训练」按钮。** 改成打勾即存：

- 任何改动 1.5 秒后自动存草稿，底下一行显示「已存 · 43% 完成」
- 完成度 ≥80% 且「还能再做几次」都填了 → 自动结算，算好下次重量
- 隔夜没结算的（完成度 ≥50%）下次打开 app 自动补结算

**新增预判趋势图。** 计划页多了一张「你会走到哪」：

- **体重**：灰虚线是计划轨迹，绿实线是你的真实 7 天均值，绿虚线是按当前速度的外推 —— 直接告诉你「按现在的速度，02/11 到 74.8 kg，比计划早 4 天」
- **力量**：所有动作相对起始重量的平均涨幅，计划线是 24 周 +20%
- 方向反了会直说：「近两周每周涨了 0.34 kg —— 方向反了，照这样到不了目标」

## 2026-09-06 · 再修四个看不懂的名字

`机器侧平举(重组)` 的「重」是多音字，本意是**重(zhòng)量大的那组**，但绝大多数人会读成「重(chóng)组」，完全是另一个意思。这类歧义在动作名里不能留。

| 原来 | 现在 |
|---|---|
| 机器侧平举(重组) | 机器侧平举 · 大重量 |
| 机器侧平举(高次数) | 机器侧平举 · 高次数 |
| 悬垂沉肩胛 | 抓杆吊着沉肩 |
| 90/90 呼吸 5 次收尾 | 仰躺呼吸 5 次收尾 |

同一个动作分成两个条目是**故意的** —— 一周练三次侧束要给三种刺激，而且两条不能同名，否则 app 的重量和进步曲线会串在一起。

## 2026-09-06 · 修一个很隐蔽的 bug：改了计划却不生效

用户反馈「顶部指标更新了，但动作名还是旧的」。根因：

```js
if (p.version >= DEFAULT_PLAN.version) PLAN = p;   // db 里存的赢
```

app 启动会读 db 的 `config/plan`，版本号**大于等于**代码里的就用存的那份。改动作名时没有把 `version` +1（一直是 12），所以每次打开，db 里那份 v12 都把新的顶掉 —— **代码改动生效，plan 内容改动全部无效**，看起来像更新没推上去。

两处修复：

1. `version` 12 → 13，让新计划赢
2. 判定改成**严格大于**：版本相等时用代码里的。教练后来推的新计划（版本更大）照常优先，但「改了 plan 忘了 +1」不会再静默失效
3. 两条 `console.info` 说明当前用的是哪一份，下次排查一眼看得见

`PLAN.schema.md` 和 `AGENT.md` 都加了这条警告。

## 2026-09-06 · 顶部第二格换成「连续」

「已减」的问题：第一天打开是 `0.0 kg`，本来想给人动力，结果先泼一盆冷水；体重涨了还会变成负数。而且训练页放体重指标本身就跑题。

换成 **连续 · N 次** —— 连续按计划练了几次，中间的休息日不算断。

代码里 `streak()` 早就写好了，而且会正确跳过计划里的非力量日（周五休息不会归零），只是一直没被用上。

还没练过时显示「从今天起」，不显示刺眼的 0。

## 2026-09-06 · 连续加火焰，越烧越旺

四级，随连续次数升级：

| 连续 | 样子 |
|---|---|
| 0 次 | 不显示火，写「从今天起」 |
| 1–2 次 | 暗黄小火苗，慢速摇曳（2.6s） |
| 3–5 次 | 橙色火焰 + 内焰，稍快（2s） |
| 6–11 次 | 橙红 + 外发光，更快（1.5s） |
| 12 次以上 | 赤红 + 白热内焰 + 强发光，最快（1.05s） |

内联 SVG，跟 app 里其它插画一个路子，没引外部资源。`prefers-reduced-motion` 下动画关掉、火焰静态显示 —— 一直跳动的东西对注意力不友好。

## 2026-09-06 · 交付物必须是 Artifact，不是 HTML 文件

真人测试第二个坑：agent 面谈做完，最后**给了用户一个 HTML 文件**。用户拿到文件是用不起来的 —— 没网址、手机打不开、记的东西下次就没了。

- `AGENT.md` 第 0 步改成「先确认交付物做不做得出来」，并写清楚**做完之后用户应该处于什么状态**：artifact 存在他自己 claude.ai 账号的云端、数据是他私有的、换设备打开还在、能加到主屏幕。达不到就不算完成
- 第 5 步改成「发布成 Artifact —— 不是给文件」，加了发布后**自查三条**（拿到网址了吗 / capabilities 带了吗 / 交出去的是网址还是代码）和一份**失败清单**（贴代码、给下载文件、说"存成网页打开"、漏 capabilities）
- 做不到时必须**明确告诉用户这是降级版**，让他自己决定换环境重来，不许默默交个文件
- README 加了用户侧的排错话术：「把它发布成 Artifact，带上 capabilities，把链接给我」

**顺带修一个我上一轮谎报的改动**：「先面谈后取模板」当时的替换静默失败了（脚本没做断言），实际没改成。这次全部替换都加了断言，失败就抛异常不写文件。

## 2026-09-06 · 饮食页减负 + 方案必须因人而异

**饮食页**

- 顶部四个营养格：手机上改成 **2×2**，数字 22px → **38px**。四格并排在 390px 屏上每格只有 77px，本来就挤，「做大」的正解是换布局不是加字号（顺带修了 grid 子项 `min-width:auto` 导致的横向溢出）
- 「还能 / 还差」从数值里挪进小标签，四格对齐
- 近 7 天热量图从 90 高压到 56，只占一条
- **「AI 估算中…」改成显眼的状态条**：转圈 + 色块 + 「正在看你吃了什么，大概 5-10 秒…」。之前只是一行灰字，用户以为页面卡住了。扫描单读数同理
- **水改成纯提醒，不再记录** —— 一天八格根本记不准。改成一段话：一个 1 L 水壶喝 4 次，尿浅黄就够了

**训练页**

- **休息倒计时切到别的页签不再消失**。之前切走就没了，回来才发现早就该做下一组

**AGENT.md：这才是仓库的价值**

加了一节「组一个专家团队，把方案吵出来」：

- 四个视角分别评估再合成（力量教练 / 运动物理治疗师 / 运动营养师 / 周期化教练），**允许打架，分歧要摆给用户看**
- **分化从条件推导**，不是照抄示例的四天分化：1–2 天全身 / 3 天推拉腿 / 4 天上下肢 / 5–6 天细分；没练过的一律从 2–3 天起步
- **24 周是周期，路线因人而异**：减脂、增肌、只调饮食、伤后恢复是四条不同的路，`blocks` / `nutrition` / `milestones` 全部重算
- **绝对不要动模板的 HTML 和 CSS** —— 手机适配已经调好，重写页面会搞出「只能上下滑、缩放乱跳、按钮点不准」（真实发生过）
- 文件开头列出**已知的四种翻车方式**，每一种都对应下面一条规矩

## 2026-09-06 · 记一餐估算失败

真人反馈：拍蛋白棒的营养成分表 → 让手动填；把成分写进文字框点「记这一笔」→ 「估算失败，手动填数字」。两条路都不通。

查下来是三个叠在一起的问题：

**一、prompt 要的东西太多，回复被长度上限截断。** 平台文档说得很明白：截断会以 `invalid_json` 拒绝，而且**不该在代码里重试，应该把指令收紧、少要点东西**。原来要逐项明细 + 克数 + 四大营养素 + 热量上下区间 + 把握度 + 备注。现在只要 `{kcal, protein, carbs, fat, name, note}` 六个字段，规则也从六条压到一句。

**二、真实错误码被兜底文案吞了。** `m[code] || "估算失败"` —— 没映射的码全变成一句没信息量的话。现在**把原始码显示出来**（`AI 回的内容没解析出来 (invalid_json)`），旁边加「再试一次」按钮。

**三、这个页面拿不到图片能力时没说清楚。** `canImage` 为 false 时只写了「照片已存，数字自己填一下」。现在明确说「这个页面没法把图片发给 AI」，并引导：把包装上的热量和蛋白抄进文字框，再点记这一笔 —— 然后光标自动跳过去。

**加了一个本地兜底解析器。** AI 失败时，直接从用户写的文字里读数字：

| 输入 | 读出 |
|---|---|
| `...with 28 g protein 150 cal zero sugar` | 150 kcal / 蛋白 28 |
| `蛋白棒 热量190大卡 蛋白质20g 碳水14g 脂肪7g` | 190 / 20 / 14 / 7 |
| `Calories 240 Protein: 24g Carbohydrate: 26g Fat: 8g` | 240 / 24 / 26 / 8 |
| `中午在外面吃了个牛肉面` | 读不出 → 才报错 |

包装食品把标签抄进去就能记上，不依赖 AI。中英文都要求数字后面跟 `g`，否则「蛋白棒 热量190」会被读成蛋白 190（踩过）。

## 2026-09-06 · 记录标题不该是你打的那一整段

写得越详细，标题越难看：整段「david peanut butter, chocolate chunk flavored protein bar with 28 g protein 150 cal zero sugar」都成了这一餐的名字。

连锁问题更麻烦：**「常吃」是按标题分组的**，标题每次都不一样，它就永远学不会你常吃什么。

- AI 现在会返回一个短名（prompt 里写明「只要品名/菜名，不要写营养数字」），**永远优先拿它当标题**。之前只在用户没打字时才用
- 你写的原文另存进 `raw`，编辑那一餐时能看到「你当时写的：…」
- AI 失败时本地缩一个名：在第一个数字或 `with / 热量 / protein / calories` 这些词处截断，再按词边界压到 28 字

| 输入 | 标题 |
|---|---|
| `david peanut butter, chocolate chunk...28 g protein 150 cal` | AI：`David 花生酱蛋白棒` / 兜底：`david peanut butter…` |
| `蛋白棒 热量190大卡 蛋白质20g 碳水14g 脂肪7g` | `蛋白棒` |
| `Chipotle chicken bowl with brown rice, 720 calories` | `Chipotle chicken bowl` |

踩了个坑：一开始「蛋白棒」被「蛋白」这个关键词从第 0 位切没了。现在关键词只在出现位置 > 2 时才算数。

## 2026-09-06 · 一次记多道菜 + 几个人分

两件事其实是同一条链路。

**拆菜**：AI 现在返回一个紧凑的逐菜清单（`items[{n,kcal,p,c,f}]`，最多 8 条，字段名用单字母省 token），每道菜**各存一条记录**，不再糊成一条。

**分食**：中餐一桌四个菜，四个人吃，算成一个人的量才有意义。

份量看着多的时候会**先问再存**，不问就不落库：

```
4 道菜,共 2700 kcal · 炒菜油量按上限
这些是你一个人吃的,还是几个人分?
[ 我一个人 ] [ 2 人分 ] [ 3 人分 ] [ 4 人分 ] [ 其他 ]
```

选 4 人分 → 四条记录各自除以 4，合计 675 kcal，每条标注「4 人分」。「其他」可以输入任意人数。

触发条件：AI 判断像一桌菜（`share: true`）、或 ≥3 道菜、或总热量 ≥1000。单人一道菜不问，照旧直接存。

踩了个坑：AI 给的菜名又被缩名函数处理了一遍，「米饭 4 碗」变成「米饭」、「鸡胸 200g」变成「鸡胸」。AI 的菜名本来就短且干净，现在只按长度截断，不做关键词切割。

## 2026-09-06 · 拍照发不出去时的兜底

用户拍了照，页面回「这个页面没法把图片发给 AI」。

查了平台约定：`sample.limits()` 的 `images` 成员**只在这个视图能发图片时才存在**，而且这个调用「开销很低、纯本地，不消耗额度，也不会弹窗」。所以这是**视图层面的事实，页面打不开这个开关**。

能做的三件事：

1. **不再开机取一次就定死。** 原来 `limits()` 在 boot 时调一次，失败就 `catch(() => null)` 永远关掉图片。现在拍照时**再问一次**（反正不花钱不弹窗），开机那次失败或时机太早的情况能救回来
2. **把结果打到控制台**（`[sample] 图片能力: …`），下次排查一眼看得见
3. **真发不出去时，给一条能马上用的路**，而不是一句"不行"：

   > 照片存好了，但这个页面不能把图片发给 AI
   > 把包装上的数字抄一句到上面那个框，再点「记这一笔」就行，比如：
   > `蛋白棒 150 cal 28g protein`
   > 数字它读得出来，不用 AI 也能记上。

   光标自动跳到输入框；拍照按钮同时改名为「📷 拍照存档」，明确它现在只做视觉留档。

照片本身照常存进记录，当日记录里那张缩略图还在。

## 2026-09-06 · 发不了图片时的一桌菜怎么办

图片能力关着的视图，拍一桌菜就卡住了。现在给两条明确的路，并且区分场景：

- **包装食品**：抄标签上的数字（本地解析器直接读，不用 AI）
- **一桌菜**：回 Claude 对话里把照片发给它，用一句现成话术，把它给的一行粘回输入框

话术做成了**一键复制**：

> 看看这桌菜有哪些，每道菜一整份的热量和蛋白估一下，只回一行，格式：菜名 xxxkcal 蛋白xxg / 菜名 xxxkcal 蛋白xxg

粘回来之后照常走拆菜 + 问几个人分的流程。

**另外把「平均分」的局限说破了。** 几个人吃饭很少是严格均分 —— 有的菜没动，有的菜自己吃了大半。分食落库后现在提示：

> 已按 3 人分记下 3 条，共 553 kcal
> 平均分只是起点 —— **某道菜你没吃就删掉，多吃了就点开改数字**。

这也是当初把每道菜拆成独立记录的意义：删一条、改一条，比重算整顿饭容易得多。
