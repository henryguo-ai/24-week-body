# 生成你自己的泳者计划

把**这一整个文件**贴给 Claude（claude.ai 或 Claude Code 都行），它会问你一组问题，然后生成属于你的版本并发布。

需要：一个 Claude 账号。全程大约 10 分钟。

---

## 以下内容贴给 Claude ↓

你是我的力量与体成分教练。我要你帮我生成一份「泳者计划」并发布成 Artifact。

### 第 1 步：拿到模板

从这个仓库读取两个文件：

- `https://raw.githubusercontent.com/henryguo-ai/swimmer-plan/main/src/index.html` —— 框架本体
- `https://raw.githubusercontent.com/henryguo-ai/swimmer-plan/main/PLAN.schema.md` —— 字段说明

`index.html` 里有一个 `const DEFAULT_PLAN = { ... }` 对象，那是虚构示例人的数据。你的任务是把它换成我的。**除了这个对象，其它代码一行都不要改。**

### 第 2 步：问我这些问题

一次问完，别一条一条来。我答不上来的你就按常见值估，估了要告诉我估的是哪些。

**身体**
1. 性别、年龄、身高
2. 当前体重（kg 或 lb 都行）
3. 体脂率 —— 做过 InBody / Evolt / DEXA 就给数字和日期；没做过就说没做过
4. 腰围、胸围、臂围（能量就量，晨起空腹、肚脐水平）
5. 有没有伤病、手术史、关节问题、慢性病、在吃的药

**目标**
6. 想到什么程度 —— 目标体重？目标体脂？还是「腰围掉几厘米」？
7. 想在多久内到（我建议 24 周，太急的目标我会告诉你不现实）
8. 减脂优先还是增肌优先？

**条件**
9. 每周能练几天？每次多长时间？
10. 在哪练 —— 商业健身房（器械齐）、家里哑铃、只有自重？有没有测过器械最大力量？
11. 有没有已经在做的有氧？游泳、跑步、走路？

**吃**
12. 有没有饮食限制 —— 素食、不吃某类、宗教、过敏、乳糖不耐
13. 平时常吃什么？外食频率高吗？
14. 会不会自己称重记录，还是只能拍照估

**姿态**（决定每天做哪些热身）
15. 有没有这些问题：骨盆前倾（站着腰特别塌）、圆肩、前伸头（脖子往前）、长期久坐

### 第 3 步：按 PLAN.schema.md 生成我的 plan

规则：

- **热量**：先按我的数据估 TEE，再定阶梯。减脂缺口每周掉 0.5–1% 体重，别更快。蛋白按**目标体重**每磅 0.9–1.1 g。脂肪不低于总热量 20%。
- **分化**：按我一周能练几天选。3 天用推拉腿，4 天用上下肢分化（示例用的是 U1 肩臂 / L 腿 / U2 背 / U3 手臂），5 天以上再细分。
- **动作**：按我的器械条件选。没有的器械换成有的，别留着我做不了的动作。有伤病的绕开，并在 `notes` 里写明绕开了什么、为什么。
- **起始重量**：按 schema 里的方法估，宁可轻。在 `notes` 里提醒我第一次做完直接改成实际重量。
- **热身**：`warmup.common` 按我的姿态问题选，从 `MOBILITY` 里挑。没有姿态问题就用通用的那几项。
- **`pose` 字段不要手写**，从 `MOBILITY` 或现有动作里复制姿势最接近的。
- **`rules` 数组基本原样保留**，只在和我的情况冲突时改（比如我不游泳就把有氧那条里的游泳换掉）。
- `milestones` 按我的目标和时间线重算。
- `notes` 写明：这份计划的思路、你估了哪些数、我需要注意什么。

生成前先把关键判断讲给我听：估的 TEE 是多少、缺口多大、预计多久到、为什么选这个分化。**我认可了你再写代码。**

如果我的目标不现实，直接说，并给我一个现实的替代方案。别为了顺着我把数字编好看。

### 第 4 步：发布

把改好的完整 HTML 发布成 Artifact，必须带上这两个能力：

```
capabilities: { db: {}, sample: {} }
```

- `db` 是同步存储，没有它我换个设备记录就没了
- `sample` 是拍照估餐和读体成分扫描单

发布后把链接给我，并告诉我：

1. 手机上怎么打开（在 claude.ai 里打开才有同步，直接开链接是单机的）
2. 第一周该干什么
3. 什么时候回来找你调计划

### 之后

我每隔几周会回来找你调。到时候我会把当时的体重趋势、围度、训练表现告诉你，你改 `nutrition` 的 `overrides` 或起始重量，重新发布到**同一个 Artifact**（用同一个 URL 更新，别新建，否则我的数据就断了）。

---

## 贴完之后

Claude 会先问你问题，再把判断讲给你听，你点头它才写代码。

**发布时记得确认它带了 `capabilities: { db: {}, sample: {} }`** —— 这是最容易漏的一步，漏了的话数据只存在当前浏览器里，换手机就没了。

**以后要更新，让它更新同一个 Artifact URL，不要新建。** 新建 = 新的存储空间 = 历史记录看不见了。
