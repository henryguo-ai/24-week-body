# 给 AI 助手的执行说明

**这个文件是写给 AI 助手看的，不是写给人看的。**

如果用户让你「按 24-week-body 里的说明生成一份身体计划」，你现在读到的就是那份说明。照着做，不要让用户自己去看 GitHub。

---

## 0. 先确认你能不能做这件事

这个计划要发布成 **Claude Artifact**，它依赖两个运行时能力：同步存储（`db`）和 AI 估算（`sample`）。

- **你是 Claude**（claude.ai 或 Claude Code）→ 继续。
- **你不是 Claude**（ChatGPT、Codex、Gemini、Cursor 等）→ **现在就告诉用户**：

  > 我可以帮你生成计划的内容，但发布不了 Claude Artifact，所以跨设备同步、拍照估算餐食、上传体成分报告自动读数这三样功能会没有，你拿到的是一个本地网页文件。想要完整版，请把同一句话发给 Claude。

  说完再问用户是要本地版还是去找 Claude。别默默降级。

## 1. 取模板

```
https://raw.githubusercontent.com/henryguo-ai/24-week-body/main/src/index.html
https://raw.githubusercontent.com/henryguo-ai/24-week-body/main/PLAN.schema.md
```

`index.html` 里有一个 `const DEFAULT_PLAN = { ... }`，那是虚构示例人的数据。**你要做的就是把这个对象换成用户的。除了这个对象，其它代码一行都不要改。**

`PLAN.schema.md` 是字段说明，生成前读完。

## 2. 面谈：一轮一问，等回答再问下一轮

**不要一次把所有问题倒给用户。** 一次一组，用户答完你再问下一组。用户不是来填表的。

用户答不上来的，你替他估，并在最后明确告诉他你估了哪几项。

---

### 第 1 轮 · 你想达到什么

> 先问你一件事就好：**你最想看到身体发生什么变化？**
>
> A. 减脂，腰围降下来，衣服穿着松一点
> B. 增肌，看起来壮一点、有型一点
> C. 调理体质 —— 精力好一点、睡得好一点、不那么容易累
> D. 改体态 —— 不驼背、脖子不前伸、腰不那么塌
> E. 说不太清，就是想健康一点
>
> 可以多选。如果多选，告诉我**最想先看到哪一个**。

选 E 的：别追问到底要什么，转而问「现在最不满意的是什么」「什么时候觉得身体不对劲」，从具体的不适反推目标。

### 第 2 轮 · 你平时怎么动

这一轮决定后面所有训练问题。**必问，不能跳。**

> 好。那你平时**怎么动**？或者说，接下来这半年你打算怎么动？
>
> A. 去健身房，器械比较齐
> B. 在家练，有哑铃 / 弹力带 / 或者纯自重
> C. 游泳、跑步、走路这类为主，不太碰器械
> D. 基本不怎么动，主要想从**吃**上调
> E. 还没想好，你给我建议

**选 D 完全合理，不要劝他去健身房。** 只需要问一句：愿不愿意每天走 20–30 分钟？愿意就写进计划，不愿意就纯饮食方案。

选 E 的：根据第 1 轮的目标 + 他的时间和条件给建议，然后确认他接受哪个。

### 第 3 轮 · 按上一轮分支追问

**只问和他相关的。** 选 D 的人不要问他哑铃多重。

**A 健身房**
> 每周能去几天？每次大概多久？
> 有没有测过力量（eGym、体测、教练带你测过 1RM）？有数字就发我。
> 有没有特别不想练的部位，或者练了会不舒服的动作？

**B 在家**
> 家里有什么？哑铃（多重？一对还是单个？可调吗？）、弹力带、引体杆、长凳、瑜伽垫？
> 有没有能躺下、能站直举手臂的空间？
> 每周能练几天，每次多久？

**C 游泳 / 跑步 / 走路**
> 主要是哪一项？每周几次，每次多久？
> 如果是游泳：会哪几种泳姿？能连续游多久？
> 愿不愿意每周加 1–2 次简单的力量动作（自重就行）？不愿意也完全可以。

**D 主要靠吃**
> 跳过训练问题，直接进第 4 轮。

### 第 4 轮 · 身体基础

> 接下来是几个数字，有就给，没有我来估：
>
> 1. 性别、年龄、身高
> 2. 现在的体重（kg 或 lb 都行）
> 3. 测过体成分吗？InBody、Evolt、DEXA、或者家里的体脂秤 —— 有的话给我体脂率和测量日期
> 4. 腰围量过吗？没量过也没关系

**如果没有腰围**，教他量，这是整个计划里最有用的一个数：

> 晨起、空腹、站直放松、正常呼气末，皮尺贴着皮肤但不勒紧，肚脐水平绕一圈，量 3 次取平均。
> 这个数比体重诚实 —— 体重会因为水分上下晃两三斤，腰围不会。

**如果没有体脂率**，按性别、年龄、BMI 和他对自己身材的描述估一个区间，明确说这是估的。

### 第 5 轮 · 吃

> 说说吃的：
>
> 1. 一天吃几顿？在家做得多还是在外面吃得多？
> 2. 有没有忌口 —— 素食、清真、过敏、乳糖不耐、不吃某类东西？
> 3. 你愿意称重记录，还是只能拍个照大概估？
> 4. 喝酒吗？含糖饮料呢？

第 3 问决定计划的严格程度：只能拍照估的人，别给他精确到个位数的克数目标，给区间。

### 第 6 轮 · 伤病与安全

**最后问，因为这一轮可能会推翻前面的方案。必问，不能跳。**

> 最后几个安全相关的：
>
> 1. 有没有受过伤、做过手术、关节有问题（膝、肩、腰、颈）？
> 2. 有没有慢性病，或者正在吃的药？
> 3. 长期久坐吗？有没有驼背、脖子往前伸、站着腰特别塌？
> 4. （女性）目前怀孕或哺乳吗？

**红线**：有慢性病、在服药、怀孕哺乳、或者伤病还没好的，**先告诉他去问医生**，然后只给保守方案 —— 不要给激进的热量缺口，不要给大重量。

第 3 问决定 `warmup.common` 放哪些每日姿态动作，从模板的 `MOBILITY` 里挑。

---

## 3. 先讲判断，用户点头你再写代码

面谈完，**不要直接开始生成**。先用大白话把这几件事讲给他听：

1. 我估你每天大概消耗多少热量，怎么估的
2. 打算给你多大的缺口（或盈余），每周大概掉/涨多少
3. 按这个速度，多久到你说的目标 —— **如果不现实，直说**，并给一个现实的替代
4. 为什么给你这个训练安排
5. 哪几项是我估的，你之后测了要告诉我改

**目标不现实就直说，别为了顺着用户把数字编好看。** 比如「三个月减 20 kg」，告诉他健康速度下这需要多久，以及为什么更快会掉肌肉。

他点头了，再往下走。

## 4. 生成 plan

按 `PLAN.schema.md` 填。四种模式对应的配置：

| 用户类型 | `order` | `pattern` | `days` |
|---|---|---|---|
| 健身房 | 力量日 key 列表，如 `["U1","L","U2","U3"]` | 力量日 + `"cardio"` + `"rest"` 混排 | 每个力量日一组动作 + `cardio` |
| 在家 | 通常 2–3 个 key，如 `["U1","L"]` | 同上 | 动作换成哑铃/弹力带/自重 |
| 游泳跑步走路 | `[]`（或加 1–2 个自重日） | 以 `"cardio"` 为主 | **只需要 `cardio`** |
| 只调饮食 | `[]` | `"cardio"` 和 `"rest"` | 只需要 `cardio`，里面放走路 |

**`order: []` 是合法的**，引擎会自动只显示有氧日。别为了填满而硬塞用户做不了的动作。

`days.cardio.ex` 里换成他真正会做的：游泳、坡度快走、爬楼机、跑步、椭圆机、骑车 —— 模板里已经有几个可以直接改。

其余要点：

- **热量**：缺口按每周掉 0.5–1% 体重定，别更快。蛋白按**目标体重**每磅 0.9–1.1 g（每 kg 2–2.4 g）。脂肪不低于总热量 20%。
- **起始重量**：宁可轻。在 `notes` 里告诉他第一次做完直接把实际重量填进 app。
- **`pose` 字段不要手写**，从 `MOBILITY` 或模板现有动作里复制姿势最接近的。
- **`rules` 数组基本原样保留**，只在和他的情况冲突时改（不游泳就把有氧那条里的游泳换掉）。
- `milestones` 按他的目标和时间线重算。
- `notes` 写清楚：这份计划的思路、你估了哪些数、绕开了什么伤病、他要注意什么。
- **忌口和过敏要落到 `QUICK_SEED` 里**（常吃食物的初始列表），别让他每天看着自己不能吃的东西。用一阵子之后这个列表会被他自己的记录顶替。

## 5. 发布

发布成 Artifact，**必须带这两个能力**：

```
capabilities: { db: {}, sample: {} }
```

- `db` —— 训练、饮食、体重记录的同步存储。**漏了这个，用户换手机记录就没了。**
- `sample` —— 拍照估餐、读体成分报告

标题用 `24 周`，或者用户自己起的名字。

**这是最容易漏的一步，发布完自己确认一遍。**

## 6. 交付时告诉用户

1. **怎么打开**：手机上在 claude.ai 里打开这个链接才有同步；直接开链接是单机的。建议加到主屏幕。
2. **第一周做什么**：具体到明天早上第一件事（称重）和第一次训练是哪天。
3. **只做三件事**：每天早上记体重；每组填次数和「最后一组还能再做几次」；每餐拍照或点常吃。其余 app 会算。
4. **什么时候回来找你**：建议每 2–4 周，带上体重趋势、围度、训练表现。
5. **更新时用同一个 Artifact URL**，不要新建 —— 新建等于新的空存储，历史记录会断。

## 7. 之后每次调整

用户回来时，让他给你：近期体重趋势、最新围度、训练完成情况、哪里觉得吃力。

然后改 `nutrition` 的 `overrides`、起始重量、或动作安排，**更新回同一个 Artifact URL**。

调整幅度：热量一次 ±100 起步。体重停滞先加有氧时长，再考虑减热量。

---

## 免责

这个模板不是医疗建议，作者不是医生也不是持证教练。有伤病、慢性病、在服药、怀孕哺乳，或者体重体脂在极端区间的用户，先让他去问医生。
